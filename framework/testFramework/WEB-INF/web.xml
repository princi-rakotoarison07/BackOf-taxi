<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
                             http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <!-- Configuration du filtre pour vérifier les ressources -->
    <filter>
        <filter-name>ResourceFilter</filter-name>
        <filter-class>framework.utilitaire.ResourceFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>ResourceFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Paramètre de configuration pour @Authorized : nom de l'attribut de session qui signifie « connecté » -->
    <context-param>
        <param-name>auth.session.attribute</param-name>
        <param-value>user</param-value>
    </context-param>

    <!-- Paramètre de configuration pour @Role : nom de l'attribut de session qui stocke le rôle courant -->
    <context-param>
        <param-name>role.session.attribute</param-name>
        <param-value>role</param-value>
    </context-param>

    <servlet>
        <servlet-name>FrontServlet</servlet-name>
        <servlet-class>framework.servlet.FrontServlet</servlet-class>
        <multipart-config>
            
            <location></location>
            
            <max-file-size>-1</max-file-size>
            
            <max-request-size>-1</max-request-size>
            
            <file-size-threshold>0</file-size-threshold>
        </multipart-config>
    </servlet>

    <servlet-mapping>
        <servlet-name>FrontServlet</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>

</web-app>
